services:
  - type: web
    name: shl-assessment-recommender
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: GEMINI_API_KEY
        sync: false  # must be set manually in dashboard
      - key: PORT
        value: 10000
